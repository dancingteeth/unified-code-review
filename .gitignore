# Local drafts and maintainer material (never part of the installed skill).
# `npx skills add <owner>/<repo>` ships only tracked files, so anything ignored
# here is excluded from the pack users receive.
PLAN.md
_plan/
reference/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local install / CLI scratch
.skills/
node_modules/
