# unified-code-review

Agent skill: full code-review rubric (risk triage → agent-authored checks → call-graph pincer → structural bar).

Install by copying or symlinking `SKILL.md` into your agent skills directory (e.g. `~/.cursor/skills/unified-code-review/`).

See `SKILL.md` for the rubric.
